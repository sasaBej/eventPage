import { Box, styled } from '@mui/material';

export const StyledBoxEventDetails = styled(Box)(() => ({
  backgroundColor: '#fff',
  boxShadow: '0 4px 8px 0 #e8f1fc',
  height: '100%',
  borderRadius: '8px',
  padding: '35px'
}));
